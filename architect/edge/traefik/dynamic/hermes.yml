# Dynamic routes — Hermes chat LB (HTTP, no TLS)
# Upstream `hermes` service DNS load-balances across HERMES_REPLICAS (Medium/High default 2).
http:
  routers:
    hermes-gw:
      rule: "PathPrefix(`/`)"
      entryPoints:
        - web
      service: hermes-gw
      priority: 1

  services:
    hermes-gw:
      loadBalancer:
        servers:
          - url: "http://hermes:8642"
        healthCheck:
          path: "/"
          interval: "15s"
          timeout: "5s"
        passHostHeader: true
