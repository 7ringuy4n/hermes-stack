# Case: web search weather (High)

Query current weather in Ho Chi Minh City.

- Search backend invoked (SearXNG and/or configured web backends).
- Results are live search hits, not an empty fake.
- If search is down, short controlled error (no crash).
