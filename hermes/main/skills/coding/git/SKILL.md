---
name: git
description: "Git branching, worktrees, finishing branches. Use for rebase, cherry-pick, merge vs rebase, worktree workflows, branch cleanup."
---

# Git workflows

## Load when relevant

| Task | Vendor skill |
|---|---|
| Parallel work / worktrees | `vendor/superpowers/using-git-worktrees` |
| Merge/rebase decision at end | `vendor/superpowers/finishing-a-development-branch` |

## Hermes operator rules

- Commits/PRs only when user asks.
- No force-push to main; no `--no-verify` unless requested.
- See user/repo rules for commit message style.

## Sources

obra/superpowers (MIT); Kodus catalog references `git-advanced-workflows` (catalog only).
