---
name: comfyui
description: "REMOVED — ComfyUI is not used on this stack. Redirect to image-gen (router combo image-gen)."
---

# ComfyUI removed

Do **not** run Comfy workflows, CLI, or `comfyui` scripts.

Still images: skill **`image-gen`** → `POST http://dispatcher:8090/v1/image` (Omni/9Router combo `image-gen`).

Video / music / audio: skill **`video-gen`** (refuse).
