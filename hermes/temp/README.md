# hermes / temp

**Not committed** (`hermes/temp/**` in root `.gitignore`).

| Path | Purpose |
|---|---|
| [`skills/`](./skills/README.md) | Live-server skill pack (moved out of `main`) — promote into `hermes/main/skills/` when ready to ship |
| (other) | Host notes, one-off experiments |

**Default runtime path is always `hermes/main/`** — not this folder.
