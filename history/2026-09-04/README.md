# 2026-09-04 — stream_options + OpenBao/refuse/compact wave

## Core (merged main earlier)
- update-omnirouter preserves operator Omni combo membership
- image-gen resolves concrete members (Requested Model + bypass combo gating)
- router drops stream_options after forced non-stream (OpenCode/GLM 400 fix)
- classify media multi-purpose live-scene (EN); unique media filenames
- Pollinations device-flow helper; vision trusts supportsVision

## Core (this wave)
- Host `video_gen` refuse → dispatcher `/v1/video-policy-refuse` (no scenic image-gen)
- OpenBao case 43 + `openbao_kv_lab` (store / update / scrub / load fill)
- Memory `/v1/compact` + `EMBED_MODEL=embedding`; search/embed send `model` for Omni UI
- Classify refuse: URL video/music never invents SCENE

## VPS gate (PASS)
- Concurrent scenic Hanoi image + Da Nang weather PDF (earlier)
- OpenBao KV store/update/scrub/load fill PASS
- Embedding combo (dim 1024, member model echoed) + memory `/v1/compact` PASS
- YouTube URL Tn inject: refuse, **0** new images PASS

## Merge
HARDEN gate PASS for this wave — proceed MR develop then main.
